# Use Audio as the root content item

The product models `Audio`, rather than `Interview`, as the persistent item shown in its libraries and editorial workflow. A journalist may capture either an interview or a personal audio note, so treating every item as an Interview would impose incorrect semantics; the Spanish interface calls the item `Audio` and the English interface calls it `Recording`, without adding note-specific workflows to the MVP.
